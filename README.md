# breakout_assignment
SOP based chatbot. For openAI to work, add an .env file containing the API key ('dotenv' library has been used).

The main SOP was a pdf, that later converted to text file using an online tool, just to keep the code simple.

Requirements include, 'langchain', 'langchain_openai'




